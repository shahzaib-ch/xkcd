package my.test.xkcd

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Tests methods in AppUtil class
 *
 * Runs on development machine (host)
 */
class AppUtilUnitTest {


    @Test
    fun appUtil_isFile() {
//        val bitmap = Bitmap.createBitmap(100, 200, Bitmap.Config.ARGB_8888);
//        AppUtil.saveBitmapToFile(
//            File(context?.filesDir, AppConstants.COMIC_SHARING_FOLDER_NAME),
//            AppConstants.COMIC_SHARING_FILE_NAME,
//            bitmap,
//            Bitmap.CompressFormat.PNG, 100
//        )
        assertEquals(4, 2 + 2)
    }
}
